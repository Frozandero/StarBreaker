# UI Fallback Register

> Satellite doc — the authoritative process is `docs/ui-workflow.md`; commands/tools/data live in `docs/ui-reference.md`.

This register tracks active UI fallbacks in `starbreaker-ui` and explicitly records owner, scope, trigger signal, and retirement target.

## Active Fallbacks

| Component | Fallback | Owner | Scope | Trigger Signal | Retirement Target |
| --- | --- | --- | --- | --- | --- |
| `crates/starbreaker-ui/src/bb_atlas.rs` | Manufacturer/gen atlas fallback paths | UI pipeline | Asset resolution when exact texture path is missing | `fallback_counter` in pipeline diagnostics and unresolved asset refs | Replace with authoritative source path resolution for all certified families in Phase 6B |
| `crates/starbreaker-ui/src/bb_loc.rs` | Ordinal localization parameter fallback | UI data resolution | Missing explicit named localization parameters | Localization fallback counters + unresolved localization warnings | Retire when localization map coverage proves complete on certification sets |
| `crates/starbreaker-ui/src/defaults.rs` | Versioned static default-value registry | UI data policy | Placeholder values for unresolved bindings/localization | `fallback_counter` diagnostics + unresolved binding warnings | Replace with authoritative runtime/default source extraction after fallback trigger rate is near-zero |
| `crates/starbreaker-ui/src/style/loader.rs` (`neutral_fallback`) | NEUTRAL white-on-black style when no `BuildingBlocks_Style` record (or palette slot) resolves — deliberately not a brand palette so missing data is visibly unstyled | UI style selection | Missing/invalid style source resolution | Selected style provenance + fallback warnings ("using neutral fallback") | Retire once style selection is always sourced from canonical style data for certified families |
| `crates/starbreaker-ui/src/bb_layout.rs` | Unknown sizing behavior defaults to fill-parent | Layout resolver | Unknown `sizingBehavior` values | Layout warning telemetry and unusual rect diffs in certification output | Replace with explicit sizing behavior coverage + hard-fail in non-release checks |
| `crates/starbreaker-ui/src/text/swf_draw.rs` (`swf_space_advance_px`) | Word-space advance for SWF text = the font's own space-glyph advance (data-backed); `0.33×em` only as a fallback when the font has no space glyph | Text renderer | Every space character | Width drift on multi-word text vs the reference | Re-derived 2026-06-08 against the in-game reference: the Clipper medical header space matches the font's space-glyph advance (~0.22×em); the previously-tuned `0.33×em` rendered the header space ~50% too wide (22px vs 15px). Now uses the font advance directly (the `0.33` fallback is retired to a no-space-glyph guard). |
| `crates/starbreaker-ui/src/ir_compose/engine_parts/engine_01.part` (`LABEL_CAPTION_PAIR_FLEX_ROW_SPACING = -8.0`) | Caption-pair label→value stack overlap standing in for the engine's line-box stacking of the flex Column (the decompiled flex carries `rowSpacing = 0`; the visible handoff comes from engine line-box geometry not present in DataCore) | UI compose | Every `ComponentLabelCaptionPair` value rect | Label→value gap drift vs reference (MEDGELS→200/200 top-to-top ≈28px on the Clipper medical capture) | Calibrated 2026-06 (was −5 sourced from the template's `FlexRowSpacing`; re-measured to −8 with the data-backed font model). Retire when the engine's caption-pair line-box stacking is derived from font metrics instead of a fixed overlap. |
| `crates/starbreaker-ui/src/ir_compose/engine_parts/engine_01.part` (`INLINE_NESTED_TEXTFIELD_WORD_GAP = 0.33`) | Word-space gap between an inline-chained parent text field and its nested child (e.g. header `TierLevel` "T3" → `MachineTypeNameText`) | UI compose | A `widget_text_field` nested in a same-style parent (pivot.x≈1, anchor.x>1, Center valign) | T3→MEDICAL gap drift vs reference | Replaces the previous `anchor_to_parent_x × size` (0.5×size ≈ 28px → 26px gap, ~1.7× too wide). The inline child sits one word-space after the parent text; 0.33×em (the engine word gap) lands T3→MEDICAL at ~16px (reference ~17px). Calibrated because the inline path's `parent_width`/`parent_rect.x` geometry doesn't reproduce a raw glyph space-advance. Retire if the inline gap is derived from the same measured space metric as in-word spacing. |
| `crates/starbreaker-ui/src/ui_ir/engine_parts/engine_02.part` (`LONG_HEADING1_PROMPT_FONT_SIZE = 28.7`) | Long (≥32-char) `Heading1` in a short field (≤120px) renders at a reduced prompt size | UI font sizing (fallback ladder for secondary/payload text) | Long Heading1 prompts overflowing short fields | font-size harness drift on prompt texts | Same family as the fixed-band fill: attempt the authored-entry derivation when the text-format route lands (an authored brand entry probably carries the real size); retire or re-derive then. |

| `crates/starbreaker-ui/src/ir_compose/engine_parts/engine_01.part` (`TEXT_RENDER_SIZE_CALIBRATION = 1.5`) + `src/bb_layout/engine_parts/engine_02.part` (`LAYOUT_TEXT_MEASURE_CALIBRATION = 1.5`) | TTF-fallback (DejaVu) text draws/measures at 1.5× the nominal BB size to match observed engine output; the layout constant mirrors the compose one so measure == draw | Text renderer + layout intrinsics | Every text element rendered through the bundled TTF fallback (no imported SWF font) | Width/height drift on TTF-rendered text vs references; font-size harness | Derive from real font metrics: the SWF draw-metrics measure (`pipeline/text_measure.rs`, 2026-06-11) killed the estimate for SWF-backed text; extending the same shared-metrics approach to the TTF path (units_per_em, ascent+descent are parsed) retires both constants together (remediation plan Phase 2). |
| `crates/starbreaker-ui/src/ir_compose/engine_parts/engine_01.part` (`SWF_TEXT_RENDER_SIZE_CALIBRATION = 0.84`) | Imported-SWF-font text size calibration applied at compose | Text renderer | Text drawn through imported SWF fonts on the calibrated paths | Width/height drift on SWF-rendered text vs references; font-size harness | Re-derive from the SWF font's own em model (ascent+|descent| vs units_per_em — the data-backed font model already encodes this for sizing; the residual 0.84 should fall out of the same numbers) — remediation plan Phase 2. |

| `crates/starbreaker-ui/src/mfd_view.rs` (`HOST_CONTENT_INSET = 44.0`, `HOST_STAGE_SIZE = (1280, 720)`) | GFx host content-view placement: 44px in from left/right/bottom of the host stage, flush top | MFD frame composition | Every framed MFD content view | Content-view position drift vs MFD captures | Measured framework constant: the placement is runtime ActionScript — PROVEN absent from records and static SWF placement (see the constant's doc; x/y-derived scales agree within 0.4% on the power capture). `HOST_STAGE_SIZE` could additionally be read from the host SWF header (`SwfAssetLibrary::stage_size`, as `host_stage_text_scale` already does) — do that when next touching mfd_view. Retire the inset only if a data source for the view placement is ever found. |
| `crates/starbreaker-ui/src/bb_layout/engine_parts/engine_01.part` (`ADDITIVE_ALTERNATE_REVERSE_POSITION_PHASE_RATIO = 2/3`) | Static-render sampling phase for additive AlternateReverse looped animations (where in the loop the at-rest frame is frozen) | Layout animation sampling | Additive AlternateReverse looping position animations in static renders | Animated-element position drift vs at-rest captures | Derive the at-rest phase from the authored timeline (sample at the authored rest keyframe / phase metadata) instead of a fixed ratio — remediation plan Phase 4. |

## Recently Retired / Reduced Fallbacks

| Component | Previous Fallback | Status | Evidence |
| --- | --- | --- | --- |
| `crates/starbreaker-ui/src/ui_ir/engine_parts/engine_02.part` | `fixed_band_heading_prompt_size` / `FIXED_BAND_HEADING_FILL = 0.381` calibrated banner-prompt reduction | Retired (2026-06-12, with the text-format route) | The 0.381×105 ≈ 40.0px output was reverse-engineering the AUTHORED mainmenu bioc entry `FontSize=40` (Parent(UI_Generic_Flag_01)), now applied via the textfield text-format route. Deleted; `ui_check.sh --full` ALL GREEN incl. font harness 26/26 — zero drift. |
| `crates/starbreaker-ui/src/style/loader.rs` | "Drake amber" fallback palette (invented colours present in no DataCore record), also leaked into per-slot defaults when parsing real records | Retired (commit 5bf1d7f84, 2026-06-12) | Replaced by `neutral_fallback` (white-on-black, no palette); the `rgba_colour_literals_are_not_hardcoded` guard bans reintroduction. |
| `crates/starbreaker-ui/src/ui_ir/...` | `CAPS_FORCED_UPPER_FONT_SCALE = 0.98` all-caps reduction | Retired (2026-06; see project memory "caps reduction removed" and `docs/ui-workflow.md` §10) | The width rationale was false (the reference RTT is ~2% small); styled text renders the full brand nominal; all four standards re-frozen. |
| `crates/starbreaker-ui/src/pipeline.rs` | Pipeline-local defaults builder and split fallback policy | Retired | Consolidated to `DefaultValueRegistry::with_pipeline_defaults(...)` in Phase 4 |
| `crates/starbreaker-ui/src/ir_compose.rs` | Name/path-based screen/manufacturer hardcoded rendering branches | Retired | Eliminated in Phase 2B source-backed IR pass and guarded by `.github/scripts/check_ui_hardcoding.sh` |
| `crates/starbreaker-ui/src/pipeline/swf_selection/flash_paths.rs` | Hard-coded `brand_ship_subdirs` / `annunciator_ship_subdirs` ship lists for SWF path probing | Retired (Phase 1, 2026-06-05) | Replaced by `p4k_ship_subdirs` which enumerates P4K brand dir dynamically; no hard-coded ship names remain. |

## Operational Policy

- Every fallback in production must be listed here.
- New fallbacks must declare scope, trigger, and sunset target before merge.
- Fall-backs without telemetry or retirement criteria are not allowed.
- When a fallback is removed, move it to the retired section with evidence.

## Pinned at-rest registry values

Pinned (non-derived) values in
`crates/starbreaker-ui/data/default_value_registry_v1.json` are themselves
documented fallbacks: each path family's provenance and sunset condition is
tracked in `crates/starbreaker-ui/data/default_value_registry_v1.notes.md`
(updated in the same commits that touch the registry).
